import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.midori.ai.autofighter',
  appName: 'AutoFighter',
  webDir: '../../frontend/build'
};

export default config;
